services:
  grafana:
    labels:
      - traefik.http.routers.grafana.rule=Host(`${GRAFANA_DOMAIN:-${DOMAIN}}`) && PathPrefix(`${GRAFANA_PATH:-/grafana}`)
      - traefik.http.routers.grafana.entrypoints=${TRAEFIK_ENTRYPOINTS:-http}
